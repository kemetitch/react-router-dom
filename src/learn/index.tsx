const QuickStart = () => {
  return (
    <div>
      <h1 className="text-3xl mb-2">QuickStart</h1>
      <p>
        be as small as a button, or as large as an entire page. React components
        are JavaScript functions that return markup:
      </p>
    </div>
  );
};

export default QuickStart;
